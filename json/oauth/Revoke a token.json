{
  "summary": "Revoke a token",
  "method": "POST",
  "router": "/oauth/revoke",
  "description": "Revoke an access token to make it no longer valid for use.\n\n",
  "returns": "empty object",
  "oauth": "Public",
  "request": [
    {
      "name": "client_id",
      "paramType": "formData",
      "comment": "REQUIRED String. The client ID, obtained during app registration."
    },
    {
      "name": "client_secret",
      "paramType": "formData",
      "comment": "REQUIRED String. The client secret, obtained during app registration."
    },
    {
      "name": "token",
      "paramType": "formData",
      "comment": "REQUIRED String. The previously obtained token, to be invalidated."
    }
  ]
}