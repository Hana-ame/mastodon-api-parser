{
  "summary": "Add a status to a filter group",
  "method": "POST",
  "router": "/api/v2/filters/:filter_id/statuses",
  "description": "Add a status filter to the current filter group.\n\n",
  "returns": "FilterStatus",
  "oauth": "User",
  "request": [
    {
      "name": ":filter_id",
      "paramType": "path",
      "comment": "REQUIRED String. The ID of the Filter in the database."
    },
    {
      "name": "Authorization",
      "paramType": "header",
      "comment": "REQUIRED Provide this header with Bearer <user token> to gain authorized access to this API method."
    }
  ]
}